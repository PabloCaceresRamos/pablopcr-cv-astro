---
// Data
import { jobs } from "../../data/jobsData";
---

<section class="site-container experience mt-16 mx-auto max-w-6xl px-4">
  <h2
    class="text-4xl font-bold flex flex-col items-center gap-6 text-center whitespace-nowrap text-white md:flex-row md:items-center md:text-start"
  >
    <span class="drop-shadow-[2px_2px_0_#7836cf]">Mi Experiencia</span>
    <span
      class="hidden h-1.5 grow rounded-lg drop-shadow-[2px_2px_0_#0debd8] bg-[#7836cf] md:block"
    ></span>
  </h2>

  <div class="mt-10 space-y-10">
    {
      jobs.map((job) => (
        <div class="bg-gray-900 p-6 rounded-2xl shadow-lg drop-shadow-[4px_4px_0_#7836cf] flex flex-col md:flex-row gap-8 items-center md:items-start">
          <!-- Imagen a la izquierda -->
          <div class="w-32 h-32 flex-shrink-0 bg-white rounded-xl p-4 flex items-center justify-center">
            <img
              src={job.imagen}
              alt={`Logo de ${job.title}`}
              class="max-h-full max-w-full object-contain"
            />
          </div>

          <!-- Contenido a la derecha -->
          <div class="flex-grow">
            <div class="flex flex-col md:flex-row md:justify-between md:items-center mb-2">
              <h3 class="text-2xl font-bold text-[#69c7c7]">{job.title}</h3>
              <span class="text-gray-400 font-semibold">{job.time.join(" - ")}</span>
            </div>

            <div class="flex flex-wrap gap-2 mb-4">
              {job.tecnologias.map((tech) => (
                <span class="rounded-md bg-[#7836cf]/20 text-[#a27ed1] px-2 py-0.5 text-xs font-semibold">
                  {tech}
                </span>
              ))}
            </div>

            <p class="text-gray-300 mb-4">{job.description}</p>

            <ul class="marker:text-[#7836cf] list-disc space-y-2 pl-5 text-gray-300">
              {job.list.map((item) => (
                <li>{item}</li>
              ))}
            </ul>
            
            {job.link && (
              <a 
                href={job.link} 
                target="_blank" 
                rel="noopener noreferrer"
                class="inline-block mt-4 text-[#7836cf] font-bold hover:underline"
              >
                Ver empresa →
              </a>
            )}
          </div>
        </div>
      ))
    }
  </div>
</section>
